import React from "react";
import {Tile} from '../tile/Tile'
export const TileList = ({contacts}) => {
  return (
    <div>
    </div>
  );
};



// contacts.map((contact) => {
// return <Tile name={contact}/>
// })